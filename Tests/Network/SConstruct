# TODO: should not import
Import("test_env")

test_env.Program("simple_packet_transfer",
		["simple_packet_transfer.cpp"],
		LIBS = ["framework_network", "basic", "event", "framework_threads", "pthread", "packets", "log4cxx"],
		CPPPATH = ["../../Framework", "../../Protocol", "../../include"],
		LIBPATH = ["../../lib"],
		)

test_env.Program("echo_server",
		["EchoServer.cpp"],
		LIBS = [
		"framework_network", "basic", "event",
		"framework_threads", "pthread", "packets",
		"llpackets", "log4cxx", "boost_thread-mt"],
		CPPPATH = ["../../Framework", "../../Protocol", "../../include"],
		LIBPATH = ["../../lib"],
		)

